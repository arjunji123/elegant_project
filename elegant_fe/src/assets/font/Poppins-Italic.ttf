import { StyleSheet, Text } from 'react-native';

// ...
  <Text style={styles.regularText}>
    This text uses Poppins Regular.
  </Text>
  <Text style={styles.boldText}>
    This text is bold and uses Poppins Bold.
  </Text>
// ...

const styles = StyleSheet.create({
  regularText: {
    fontFamily: 'Poppins-Regular',
    fontSize: 16,
  },
  boldText: {
    fontFamily: 'Poppins-Bold',
    fontSize: 16,
    fontWeight: 'bold', // Note: You should still use fontWeight for better platform compatibility
  },
});